pipeline {
    agent {
        label 'zip-job-docker'
    }
    stages {
        stage ('execute repo test') {
            steps {
                echo "Allahu Akbar!!!"
            }
        }
    }
}
